define(function(require) {
    var api = require('escl/api'),
        tlist = require('escl/tlist'),
        record = require('escl/record');

    return {
        api: api,
        tlist: tlist,
        record: record,
    };
});