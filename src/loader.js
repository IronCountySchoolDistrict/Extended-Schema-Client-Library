/*global require*/
require.config({
    baseUrl: '/scripts',
    paths: {
        // app modules
        config: 'Extended-Schema-Client-Library/config',
        api: 'Extended-Schema-Client-Library/api'
    }
});